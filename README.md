# Speech Voice Cloning Bot

## Описание
Этот проект предоставляет Telegram-бота, который обучается на голосовых сообщениях пользователя и синтезирует речь с тем же тембром. Бот управляет процессом сбора образцов, объединяет записи в единый голосовой профиль и использует модель [XTTS v2](https://github.com/coqui-ai/TTS) для генерации озвучки на русском языке.

## Возможности
- Регистрация пользователей и отслеживание состояния их сессии.
- Сбор голосовых сообщений в формате OGG/Opus и автоматическая конвертация в WAV.
- Нормализация и объединение загруженных образцов в единый голосовой профиль.
- Генерация речи на русском языке на основе текста с использованием пользовательского профиля.
- Автоматическая выдача сгенерированного аудио пользователю в чате.

## Установка
1. Склонируйте репозиторий и перейдите в каталог проекта.
2. Установите Python 3.10 или новее.
3. Создайте и активируйте виртуальное окружение:
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # Windows: .venv\\Scripts\\activate
   ```
4. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
5. Создайте файл `.env` и добавьте токен Telegram-бота:
   ```env
   BOT_TOKEN=ваш_токен
   ```
6. Убедитесь, что в системе установлен `ffmpeg`, необходимый для работы `pydub` и обработки аудио.

## Использование
1. Запустите бота:
   ```bash
   python main.py
   ```
2. В Telegram откройте вашего бота и нажмите «/start».
3. Следуйте подсказкам в интерфейсе:
   - «🎙 Начать обучение» — включение режима приёма голосовых образцов.
   - «🛑 Завершить обучение» — создание голосового профиля из собранных записей.
   - «🗣 Сгенерировать» — отправка текста для озвучки вашим голосом.
4. Получите сгенерированное сообщение с озвученным текстом в WAV-формате.

## Структура проекта
```
.
├── README.md
├── audio_utils.py        # Обработка аудиофайлов и управление пользовательскими каталогами
├── db.py                 # Работа с базой данных и состояниями пользователя
├── keyboards.py          # Определение клавиатур Telegram-бота
├── main.py               # Точка входа и сценарии взаимодействия с пользователем
├── requirements.txt      # Список зависимостей проекта
├── tts_engine.py         # Обёртка над моделью XTTS v2 для синтеза речи
└── docs/
    ├── data_requirements.md
    └── roadmap.md
```

## Планы развития
Смотрите подробный список задач и приоритетов в [docs/roadmap.md](docs/roadmap.md). Среди ключевых направлений развития — улучшение качества голосовых профилей, расширение языковой поддержки и автоматизация деплоя.
